# XcodeGen spec. Generate the Xcode project with:  xcodegen generate
# Then open WoodsWhisper.xcodeproj. See README.md for full setup.
name: WoodsWhisper

options:
  bundleIdPrefix: com.woodswhisper
  deploymentTarget:
    iOS: "17.0"
    watchOS: "10.0"
  createIntermediateGroups: true

packages:
  WoodsWhisperKit:
    path: Packages/WoodsWhisperKit
  # ML SDKs — iOS/iPadOS only (not watchOS-compatible), so they're attached to the iOS
  # app target rather than the shared package.
  FluidAudio:
    url: https://github.com/FluidInference/FluidAudio.git
    branch: main
  # Whisper (CoreML) speech models — the smaller variants, selectable alongside Parakeet.
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit.git
    branch: main
  # The reusable LLM/VLM libraries (MLXLLM, MLXLMCommon, MLXHuggingFace, ...) moved out
  # of mlx-swift-examples into this dedicated repo.
  MLXSwiftLM:
    url: https://github.com/ml-explore/mlx-swift-lm.git
    branch: main
  # MLXHuggingFace's load macros expand to code that references AutoTokenizer (Tokenizers)
  # and HubClient (HuggingFace) — neither is bundled by mlx-swift-lm, so we add them.
  SwiftTransformers:
    url: https://github.com/huggingface/swift-transformers.git
    branch: main
  SwiftHuggingFace:
    url: https://github.com/huggingface/swift-huggingface.git
    from: "0.8.1"

settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.9"
    # Signing team for ALL targets (app, watch app, widget + complication extensions). Pulled from
    # the DEVELOPMENT_TEAM environment variable at generate time, so one value covers every target.
    # Use Scripts/generate.sh (auto-detects it), or run:  DEVELOPMENT_TEAM=XXXX xcodegen generate
    # Empty (plain `xcodegen generate`) falls back to setting the team per-target in Xcode.
    DEVELOPMENT_TEAM: ${DEVELOPMENT_TEAM}
    CODE_SIGN_STYLE: Automatic
    # Bundle-ID prefix for every target, from the BUNDLE_ID_PREFIX environment variable, so each
    # developer can use IDs unique to their team (the generic com.woodswhisper.* IDs are already
    # registered to someone else). Scripts/generate.sh defaults this to a unique, team-derived value.
    # WW_COMPANION_BUNDLE_ID is the iOS app's ID, referenced by the watch app's Info.plist.
    WW_COMPANION_BUNDLE_ID: ${BUNDLE_ID_PREFIX}.app

targets:
  # iOS / iPadOS app
  WoodsWhisper:
    type: application
    platform: iOS
    sources:
      - path: Apps/iOS
    dependencies:
      - package: WoodsWhisperKit
      - package: FluidAudio
        product: FluidAudio
      - package: WhisperKit
        product: WhisperKit
      - package: MLXSwiftLM
        product: MLXLLM
      - package: MLXSwiftLM
        product: MLXLMCommon
      - package: MLXSwiftLM
        product: MLXHuggingFace
      - package: SwiftTransformers
        product: Tokenizers
      - package: SwiftHuggingFace
        product: HuggingFace
      - target: WoodsWhisperWatch        # embed the watch app
      - target: WoodsWhisperWidgets      # embed the Lock Screen widget + Control extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ${BUNDLE_ID_PREFIX}.app
        INFOPLIST_FILE: Apps/iOS/Resources/Info.plist
        TARGETED_DEVICE_FAMILY: "1,2"     # iPhone + iPad
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_USER_SCRIPT_SANDBOXING: NO

  # iOS widget extension: Lock Screen "New Recording" widget + iOS 18 Control.
  WoodsWhisperWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: Apps/iOSWidgets
    dependencies:
      - package: WoodsWhisperKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ${BUNDLE_ID_PREFIX}.app.widgets
        INFOPLIST_FILE: Apps/iOSWidgets/Info.plist
        TARGETED_DEVICE_FAMILY: "1,2"

  # watchOS app (single-target, modern SwiftUI watch app)
  WoodsWhisperWatch:
    type: application
    platform: watchOS
    sources:
      - path: Apps/Watch
    dependencies:
      - package: WoodsWhisperKit
      - target: WoodsWhisperWatchComplication   # embed the complication
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ${BUNDLE_ID_PREFIX}.app.watchkitapp
        INFOPLIST_FILE: Apps/Watch/Resources/Info.plist
        WATCHOS_DEPLOYMENT_TARGET: "10.0"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

  # watchOS complication: bespoke "New Recording" face.
  WoodsWhisperWatchComplication:
    type: app-extension
    platform: watchOS
    sources:
      - path: Apps/WatchComplication
    dependencies:
      - package: WoodsWhisperKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ${BUNDLE_ID_PREFIX}.app.watchkitapp.complication
        INFOPLIST_FILE: Apps/WatchComplication/Info.plist
        WATCHOS_DEPLOYMENT_TARGET: "10.0"

schemes:
  WoodsWhisper:
    build:
      targets:
        WoodsWhisper: all
        WoodsWhisperWatch: all
    run:
      config: Debug
  # Run the watch app directly on an Apple Watch.
  WoodsWhisperWatch:
    build:
      targets:
        WoodsWhisperWatch: all
    run:
      config: Debug
